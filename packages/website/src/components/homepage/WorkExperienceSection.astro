---
const formatDate = (date: Date | null): string => {
  if (!date) {
    return 'Current'
  }

  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
  })
}
---
<section class=" border-zinc-700 pr-4">
  <a href="/projects">
    <p class="border-b text-lg font-bold mb-2 border-zinc-700 text-zinc-300">Work Experience</p>
  </a>

  <ul class="">
    <li>Lead Bruh Engineer</li>
  </ul>
</section>