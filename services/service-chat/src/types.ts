import type { Message, ContentCard, Conversation } from "@prisma/client";
import type { ServerMessage, ClientMessage } from "./runtime/runtime";

export type {
    Message,
    ContentCard,
    Conversation,
    ServerMessage,
    ClientMessage
}