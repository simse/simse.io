fuse:
  dir: "/litefs"

data:
  dir: "/var/lib/litefs"

exit-on-error: false

lease:
  type: "static"
  advertise-url: "http://lhr.${FLY_APP_NAME}.internal:20202"
  candidate: ${FLY_REGION == "lhr"}

