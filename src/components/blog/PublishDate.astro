---
interface Props {
    published: Date;
    updated?: Date;
}

const { published, updated } = Astro.props;

const formatDate = (date: Date) => {
    return new Intl.DateTimeFormat('en-US', {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
    }).format(date);
}
---
<span class="text-zinc-300">Published on <span class="text-zinc-50">{formatDate(published)}</span></span>