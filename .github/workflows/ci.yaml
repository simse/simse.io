name: The whole gang
on:
  push:
    branches:
      - master
  repository_dispatch:
    types: 
      - build

jobs:
  infrastructure:
    runs-on: ubuntu-latest
    name: Set up infrastructure
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Cache .terraform
        uses: actions/cache@v2
        with:
          path: './.terraform'
          key: terraform_cache

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 0.15.0
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        id: init
        run: terraform init -var-file=prod.tfvars -backend-config=prod.hcl

      - name: Terraform Apply
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        run: terraform apply -auto-approve -var-file=prod.tfvars

  #build_site:
  #  runs-on: ubuntu-latest
  #  name: simse.io
  #  needs: infrastructure
  #  defaults:
  #    run:
  #      working-directory: ./www
  #  steps:
  #    - name: Check out repository code
  #      uses: actions/checkout@v2
#
  #    - name: Configure AWS Credentials
  #      uses: aws-actions/configure-aws-credentials@v1
  #      with:
  #        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #        aws-region: us-east-1
#
  #    - name: Install Node v14
  #      uses: actions/setup-node@v2
  #      with:
  #        node-version: '14'
#
  #    - name: Cache node_modules
  #      uses: actions/cache@v2
  #      with:
  #        path: './www/node_modules'
  #        key: simse-io-node-modules
#
  #    - name: Gatsby cache
  #      uses: actions/cache@v2
  #      with:
  #        path: './www/.cache'
  #        key: www_gatsby_cache
#
  #    - name: Install Yarn dependencies
  #      run: yarn
#
  #    - name: Build site
  #      run: yarn build
#
  #    - name: Publish site
  #      run: |
  #        aws s3 sync ./public s3://simse.io

  build_labs:
    runs-on: ubuntu-latest
    name: labs.simse.io
    needs: infrastructure
    defaults:
      run:
        working-directory: ./labs/www
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Install Node v14
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Cache node_modules
        uses: actions/cache@v2
        with:
          path: './labs/www/node_modules'
          key: labs_cache

      - name: Install Yarn dependencies
        run: yarn

      - name: Build site
        run: yarn build

      - name: Publish site
        run: |
          aws s3 sync ./public s3://labs.simse.io

  build_skunkwork_labs:
    runs-on: ubuntu-latest
    name: labs.simse.io/skunkworks
    needs: infrastructure
    defaults:
      run:
        working-directory: ./labs/skunkworks
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Install Node v14
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Cache node_modules
        uses: actions/cache@v2
        with:
          path: './labs/skunkworks/node_modules'
          key: simse-labs-skunkworks-node-modules

      - name: Install Yarn dependencies
        run: yarn

      - name: Build site
        run: yarn build --prefix-paths

      - name: Publish site
        run: |
          aws s3 sync ./public s3://labs.simse.io/skunkworks

  build_bar_maker_labs:
    runs-on: ubuntu-latest
    name: labs.simse.io/bar-maker
    needs: infrastructure
    defaults:
      run:
        working-directory: ./labs/bar-maker
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Install Node v14
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Cache node_modules
        uses: actions/cache@v2
        with:
          path: './labs/bar-maker/node_modules'
          key: simse-labs-bar-maker-node-modules

      - name: Install Yarn dependencies
        run: yarn

      - name: Build site
        run: yarn build

      - name: Publish site
        run: |
          aws s3 sync ./dist s3://labs.simse.io/bar-maker